spring.application.name=Uber-AuthService
spring.datasource.url=jdbc:mysql://localhost:3306/uber_db_local
spring.datasource.username=root
spring.datasource.password=mysql
spring.jpa.show-sql=true
# 1. STOP Hibernate from trying to manage the schema now that it's created
spring.jpa.hibernate.ddl-auto=update


server.port=9292
# 2. Tell Flyway to initialize its history table for an existing schema.
##    This will create the flyway_schema_history table.
#spring.flyway.baseline-on-migrate=true

